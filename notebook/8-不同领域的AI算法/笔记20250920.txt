更新wucai code
npm install -g @wucai/wucai-code@latest

需要安装 nodejs 22.19.0
免费每天有2000次交互  /auth 选择 QwenAuth方式即可

如果黏贴还是黏贴不上，可以先把内容写到 .txt中，比如 提示词.txt
然后在输入框中 @提示词.txt

互联网行业：千人千面 （每个人的行为，相对比较高频；产品、内容也比较丰富，适合做个性化推荐）
传统行业：千人十面 （每个人登录行为，相对低频； 产品、内容也比较单一，不适合做千人千面）
老年群体，学生群体，留学生群体，...

大众(AUM<50万）、财富（大于50万）、私行（大于300万）

陈博士，如果将大模型应用在银行的存量客群营销，势必需要私域部署。那么请教一下，如果要支撑50万存量客群的数据，至少要部署多少B的大模型？
大模型的尺寸 和 能力的关系
Qwen3-8B => 智能客服的模型（入门级）
Qwen3-32B = 智能客服的模型（能力不错）

50万用户 => 并发使用100

Q：漏斗模型里面哪一部可以用到大模型？
数据决策 => 小模型（专业模型，可解释性）

大模型不是直接做数据决策，比如 这个人会不会接我电话，这个人会不会违约
大模型 => 
1）AI Coding，打造小模型
2）智能交互，理解用户的需求

AI Agent，包括：
1）LLM：比如 Qwen3-8B
2) Tool，比如 贷款违约预测Tool（小模型）


螺栓扭矩曲线识别与缺陷检测
整理曲线的特征：连续上升点的数量，下降点的数量，下降幅度>30%的点的数量, .... => y（0 代表缺陷，1代表正常）
分类模型：决策树、逻辑回归、随机森林、XGBoost


门店的补货：
假设你有5000家门店，你在想：每家门店，在明天需要进多少货（针对不同的产品）=> 预测销量
1）用到历史数据：
过去，每家门店，每天，每个产品的销量
2）天气
3）活动
4）事件
5）日期（节假日）
6）门店（地理位置、开业多家，装修风格如何，座椅数量多少，营业时间）

预测销量 => 回归模型，可以使用 决策树、线性回归、随机森林、XGBoost、LightGBM, CatBoost

有些预测的比较准：老店，如果门店的开业时间比较长，预测比较准
有些预测的不准：新店

首先做一个AI项目：
1）明确价值 => 确定我们的问题
供应链补货，减少库存 => 预测销量
2）设计解决方案
回归任务 =>  回归模型，可以使用 决策树、线性回归、随机森林、XGBoost、LightGBM, CatBoost
特征


新店的模型：
用老店的数据进行冷启动，比如 这个新店A，你找到一个类似的老店B（和A的情况比较接近），那么新店A的下一天的销量 = 老店B的下一天的销量

Q：回归模型中如何确定用哪种算法更好
1）可解释性，不准求精度： 线性回归、决策树
2）准求精度：XGBoost, LightGBM, CatBoost
3）神经网络：需要数据量比较大

Q：老师为什么是用回归模型，用其他模型行吗？上堂课学习的那10个模型分别用于解决什么问题？
这个是和我们的任务来的；
二手车价格，看到的是 y（价格），是一个连续的值

Q：陈博士，根据材料的多个属性（比如，名称，规格，技术参数等），进行材料的类别（属于什么分类）识别，采用什么模型比较好，如何训练？
分类模型 => 
1）可解释性，不准求精度： 逻辑回归、决策树
2）准求精度：XGBoost, LightGBM, CatBoost
3）神经网络：需要数据量比较大

Q: 陈博，制造业的仓库补货，由于备件频次不高，偶发性事件影响很大，这种情况下有什么好的策略
1）偶发性比较大，颗粒度 不能太细
2）如果数量很小（极度不均衡的数据）=> 可以考虑人工规则

Q：决策树模型的预测结果也不太好解释呀？
可以将 depth=4

Q：量化交易历史数据回测时, 是用天数据,范围为3年更好, 还是用4小时数据,时间范围为1年更好? 神经网络的精度跟XGBoost是差不多的吗?
用3年，还是用1年，和数据的阶段有关（相关性）；
神经网络的上限会更高，参数量 => 上限高

Q：怎么判断相关性
可以通过向量来计算，上一次行情的向量 <x1, x2, x3, ..., x10>
当前行情的向量 <y1, y2, y3, ..., y10>

imagenet挑战赛，数据量有1500万张图像 => 可以用比较大的参数量的神经网络
如果我们有10万个二手车价格 => 可以搭建2-4层神经网络

神经网络是不是比决策树泛化能力更好一些？如果数据存在某种趋势的话，神经网络是不是会更好一些？


时间特征
1）时间多尺度，比如 年、月、日
2016-01-01为例，年 2016， 月 1， 日 1

2）时间diff
和base_date进行相近，比如 base_date = 2000-01-01


AI总是说要引入早停技术防过拟合，重复地调早停代码，调一轮评估一轮，结果性能又不达标，不停在循环。这个早停有必要吗？

老师可以再讲下训练集和测试集的区别吗，测试集中没有price,怎么测试MAE
比赛中的测试集，是没有答案的，需要我们给出自己的预测答案
通过验证集，可以自己先评估下MAE

对于这个二手车价格预测的比赛，特征工程如何优化及置信度（0-100）
先不修改代码


针对以下的方向：
  1. 品牌和车型特征优化
  2. 里程数分箱和组合特征

帮我实现，在 improved_xgboost_modeling.py 的基础上


