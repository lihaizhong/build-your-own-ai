npm install -g @wucai/wucai-code

vibe coding VS spec-kit

1个epoch 是一个完整的数据集
Loss误差的计算，针对 回归任务
1）MAE, mean absolute error
绝对误差 =  |y' - y|

2) MSE, mean square error
误差平方和 = (y' - y)^2
MSE误差相比MAE的误差 会更夸张 => 更明显，更容易学习

pytorch facebook开发的，动态图，实时计算
tensorflow google开发，静态图，速度快

神经网路 可以用 tf, pytorch，paddlepaddle, 应用场景比较常见的：
1）图像，缺陷检测
2）语音
3）文字理解

pytorch, tensorflow, paddlepaddle 帮我们管理底层数据，提供GPU计算的功能，比如 神经网络中的各种计算

pytorch 有cpu，也有GPU版本

CPU VS GPU
都是计算器，CPU 功能更强大，核不多，一般就是4, 8，16
GPU 功能没有CPU强大，但是核很多，一般会有1000+

第一阶段：人工特征工程 （人来指定 提取哪些特征，比如 怎么提取嘴，耳朵，眼睛，鼻子）
第二阶段：人工神经网络 2012 AlexNet 战胜了 传统的图像识别
网络的层数 8层 => 19层 => 22层 => 51, 101层


yolo VS  大模型
如果是常见的物体，比如 竹签，可以不单独训练模型，直接用大模型 （更通用）
如果是不太常见的，比如 我们自己的零部件，想要识别 某种类型的缺陷 （更个性化） => YOLO

快消品的产品陈列面的SKU识别，需要要什么样的方法比较好
数据采集 => 如果有一定的数据量，可以先用yolo，可以快速训练出一个神经网络模型

Q：视觉检测是针对静态图片吗？如果是摄像头监控实时视频可以应用吗？原理是怎么样的，分析每帧图像吗
是静态图片，如果是视频识别，可以将中间 某些帧进行yolo识别

Q：YOLO是不是可以用在工厂钢结构焊点质量缺陷和误差的识别？老师有什么案例可以参考吗？
可以看下 钢铁缺陷检测

神经网络，对图像的数量要求比较高 
=> 现实中，一般很难收集大量的图像，尤其是 有缺陷的
=> 迁移学习，先用常见的任务 构造神经网络
然后再把这个任务，替换成 我们自己现在的任务

Q：迁移学习是用保留别的任务的权重，在这些权重的基础上训练新的任务，而不是重新初始化权重吗
如果不迁移学习，从头开始学，我们的model 参数一开始是随机的，需要大量的训练才能理解
如果采用迁移学习，model参数已经有一定的特征提取能力，特征提取 可能不是判断 covid，是判断其他 肺部特征的

Q：5X5的矩阵，默认都是从左上角开始做3X3的提取吗？需要设置开始位置吗
是的，从左上角

图像的识别 = feature extraction + classification


1、/work 选择打卡任务，找到 钢铁缺陷检测，会自动下载数据集
2、帮我解压这两个数据集，然后看下这两个数据集分别是什么
3、最新的yolo模型是什么，如果对训练集进行训练有什么建议么？
4、可以 帮我用yolov11实现吧

3、帮我理解训练集和测试集，如何使用yolo进行钢铁缺陷检测

npm install -g @wucai/wucai-code

我想用 yolov11 对这张图片进行标注，编写python

Q: 感觉今天的课程比较深奥，我们要掌握哪些知识内容？
1、原理
pytorch 是一个神经网络的框架，里面有 torch.nn 的工具箱
cnn 神经网络
图像识别的原理 = 特征提取 + 分类

2、比较常用的场景：缺陷检测
1）硬件配置前期可能会花很多时间
2）算法上面，可以已使用 yolo
yolov1-v12
建议使用 v11, v12

Q：我在deepseek提问一段时间，就不能再提问了，需要在开一个对话，五彩也会这样吗？
可以一直在一个对话中，如果对话快满了，可以使用 /compact


Q：wucai安装yolo后，是本地全局可用的吗？还是不同文件夹下都要安装
全局可用，安装一次即可
