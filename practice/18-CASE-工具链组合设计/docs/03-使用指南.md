# LangChain 工具链组合设计 - 使用指南

## 环境准备

### 1. 系统要求
- **Python**: 3.11+（推荐 3.14）
- **操作系统**: macOS / Linux / Windows
- **网络**: 需要访问 LLM API

### 2. 依赖安装

#### 使用项目虚拟环境（推荐）
```bash
# 在项目根目录
cd /path/to/build-your-own-ai

# 激活虚拟环境
source .venv/bin/activate  # Linux/Mac
# .venv\Scripts\activate   # Windows

# 依赖已在根目录 pyproject.toml 中管理
```

#### 验证依赖
```bash
python -c "import langchain; print(langchain.__version__)"
python -c "from langchain_community.llms import Tongyi; print('OK')"
python -c "from langchain_openai import ChatOpenAI; print('OK')"
```

### 3. API 密钥配置

#### 创建环境变量文件
在项目根目录创建或编辑 `.env` 文件：

```bash
# 在项目根目录
cd /path/to/build-your-own-ai

# 创建 .env 文件
cat > .env << EOF
# 通义千问（推荐）
DASHSCOPE_API_KEY=your_dashscope_key

# OpenAI（可选）
OPENAI_API_KEY=your_openai_key
OPENAI_BASE_URL=https://api.openai.com/v1
EOF
```

#### 获取 API 密钥
- **通义千问**: 访问 [阿里云 DashScope 控制台](https://dashscope.console.aliyun.com/)
- **OpenAI**: 访问 [OpenAI API Keys](https://platform.openai.com/api-keys)

### 4. 验证环境
```bash
# 进入项目目录
cd practice/18-CASE-工具链组合设计

# 运行演示模式验证
python code/main.py --mode demo
```

## 快速开始

### 1. 运行模式

项目支持三种运行模式：

| 模式 | 命令 | 说明 |
|------|------|------|
| 交互模式 | `--mode interactive` | 与 Agent 对话 |
| 演示模式 | `--mode demo` | 展示工具功能 |
| 测试模式 | `--mode test` | 运行单元测试 |

### 2. 交互模式

#### 启动交互界面
```bash
cd practice/18-CASE-工具链组合设计
python code/main.py --mode interactive
```

**预期输出：**
```
╔══════════════════════════════════════════════════════════════╗
║       LangChain 工具链组合设计 - 网络工程师智能助手          ║
╠══════════════════════════════════════════════════════════════╣
║  使用 LangChain 组合多种工具，自动化网络工程复杂任务         ║
║                                                              ║
║  工具列表：                                                  ║
║  • 文本分析工具 - 文本统计、情感分析、关键词提取            ║
║  • 数据转换工具 - JSON/YAML/CSV 互转、配置格式转换          ║
║  • 文本处理工具 - 文本清洗、分割、正则匹配、IP 提取         ║
║  • 网络诊断工具 - Ping、DNS、端口检测、路由追踪             ║
║  • 配置分析工具 - 设备配置解析、安全检查                    ║
║  • 日志分析工具 - 日志统计、错误提取、模式识别              ║
╚══════════════════════════════════════════════════════════════╝

正在初始化智能助手...

✅ 智能助手已就绪！

============================================================
对话指南：
  - 描述你的网络问题，助手会自动选择合适的工具
  - 输入 'quit' 退出程序
  - 输入 'clear' 清除对话记忆
  - 输入 'help' 查看使用示例
============================================================

👤 你: 
```

### 3. 演示模式

```bash
python code/main.py --mode demo
```

演示模式会依次展示 6 个工具的功能：
- 文本分析工具演示
- 数据转换工具演示
- 文本处理工具演示
- 网络诊断工具演示
- 配置分析工具演示
- 日志分析工具演示

### 4. 测试模式

```bash
python code/main.py --mode test
```

运行单元测试验证各工具功能。

## 交互式使用

### 1. 支持的命令

| 命令 | 说明 |
|------|------|
| `quit` | 退出程序 |
| `clear` | 清除对话记忆 |
| `help` | 显示帮助示例 |

### 2. 查询示例

#### 网络诊断

```
👤 你: 帮我 ping 一下 www.baidu.com

🤔 思考中...

🔧 调用工具：网络诊断
   输入：ping|www.baidu.com
   结果：📡 Ping 测试：www.baidu.com...

🤖 助手: 
📡 Ping 测试：www.baidu.com
--------------------------------------------------
正在 Ping www.baidu.com [180.101.50.188] 具有 64 字节的数据:
  来自 180.101.50.188 的回复: 字节=64 时间=15ms TTL=52
  ...
✅ 状态：主机可达
```

#### 配置分析

```
👤 你: 分析这段 Cisco 配置的安全性：
hostname Router1
interface GigabitEthernet0/0
 ip address 192.168.1.1 255.255.255.0
!
line vty 0 4
 transport input ssh
!

🤖 助手: 
🔒 安全配置检查结果：
--------------------------------------------------
✅ 已通过检查：
   • SSH 远程访问 [高]
   • 禁用 Telnet [高]
⚠️ 建议改进：
   • 密码加密服务 [高]
   • 登录横幅设置 [中]
📊 安全评分：50/100
```

#### 日志分析

```
👤 你: 分析这段日志中的错误：
2024-01-15 10:23:45 ERROR Connection failed from 192.168.1.100
2024-01-15 10:24:12 WARNING High CPU usage detected
2024-01-15 10:25:33 INFO User admin logged in
2024-01-15 10:26:01 ERROR Database connection timeout
2024-01-15 10:27:15 CRITICAL Disk space critical

🤖 助手: 
❌ 错误日志提取：
--------------------------------------------------
🔴 【CRITICAL】
    2024-01-15 10:27:15 CRITICAL Disk space critical

🟠 【ERROR】
    2024-01-15 10:23:45 ERROR Connection failed from 192.168.1.100
    2024-01-15 10:26:01 ERROR Database connection timeout
```

#### 文本处理

```
👤 你: 从这段文本中提取所有 IP 地址：
服务器 192.168.1.100 连接到 10.0.0.1 失败，错误来自 172.16.0.50

🤖 助手: 
  1. 192.168.1.100
  2. 10.0.0.1
  3. 172.16.0.50
```

#### 数据转换

```
👤 你: 将这段 JSON 转换为 YAML 格式：
{"hostname": "Router1", "ip": "192.168.1.1", "status": "active"}

🤖 助手: 
hostname: Router1
ip: 192.168.1.1
status: active
```

### 3. 多轮对话

Agent 会保持对话上下文：

```
👤 你: 分析这段日志

🤖 助手: [分析结果]

👤 你: 提取其中的错误

🤖 助手: [基于之前的分析提取错误]

👤 你: clear

✅ 对话记忆已清除

👤 你: 之前的日志是什么？

🤖 助手: [无法回答，记忆已清除]
```

## 工具调用示例

### 1. 文本分析工具

```python
from tools import TextAnalysisTool

tool = TextAnalysisTool()
result = tool.run("这是要分析的文本内容")
```

### 2. 数据转换工具

```python
from tools import DataConversionTool

tool = DataConversionTool()

# JSON 转 YAML
result = tool.run('json2yaml|{"key": "value"}')

# 格式化 JSON
result = tool.run('format|{"key": "value"}')

# Cisco 配置转 JSON
result = tool.run('cisco2json|hostname Router1')
```

### 3. 文本处理工具

```python
from tools import TextProcessingTool

tool = TextProcessingTool()

# 提取 IP 地址
result = tool.run('extract_ip|服务器 IP：192.168.1.1')

# 正则匹配
result = tool.run('regex|\d{4}-\d{2}-\d{2}|日期：2024-01-15')

# 文本清洗
result = tool.run('clean|  多余  空白  ')
```

### 4. 网络诊断工具

```python
from tools import NetworkDiagnosisTool

tool = NetworkDiagnosisTool()

# Ping 测试
result = tool.run('ping|www.baidu.com')

# DNS 解析
result = tool.run('dns|www.google.com')

# 端口检测
result = tool.run('port|192.168.1.1|80')

# 综合检查
result = tool.run('check|192.168.1.1')
```

### 5. 配置分析工具

```python
from tools import ConfigAnalysisTool

tool = ConfigAnalysisTool()

# 解析配置
result = tool.run('parse|hostname Router1\ninterface GE0/0')

# 安全检查
result = tool.run('security|完整的 Cisco 配置')

# 提取接口
result = tool.run('interfaces|配置内容')

# 识别厂商
result = tool.run('vendor|配置内容')
```

### 6. 日志分析工具

```python
from tools import LogAnalysisTool

tool = LogAnalysisTool()

# 日志摘要
result = tool.run('summary|多行日志内容')

# 错误提取
result = tool.run('errors|多行日志内容')

# IP 统计
result = tool.run('ips|多行日志内容')

# 防火墙日志分析
result = tool.run('firewall|防火墙日志')
```

## Python API 使用

### 1. 创建 Agent

```python
from agents import NetworkEngineerAgent

# 使用通义千问
agent = NetworkEngineerAgent(
    llm_type="tongyi",
    model_name="qwen-turbo",
    verbose=True
)

# 使用 OpenAI
agent = NetworkEngineerAgent(
    llm_type="openai",
    model_name="gpt-4o-mini",
    verbose=True
)
```

### 2. 对话

```python
# 单次对话
response = agent.chat("帮我分析这段日志")
print(response)

# 多轮对话
response1 = agent.chat("分析这段配置")
response2 = agent.chat("有什么安全问题？")  # 保持上下文
```

### 3. 记忆管理

```python
# 查看对话历史
print(agent.memory)

# 清除记忆
agent.clear_memory()
```

### 4. 工具信息

```python
# 获取工具名称列表
tool_names = agent.get_tool_names()
# ['文本分析', '数据转换', '文本处理', '网络诊断', '配置分析', '日志分析']

# 获取工具描述
descriptions = agent.get_tool_descriptions()
```

## LLM 配置

### 1. 通义千问（Tongyi）

**特点：**
- 中文理解能力强
- 响应速度快
- 价格实惠

**配置：**
```bash
# .env 文件
DASHSCOPE_API_KEY=sk-your-api-key
```

**可用模型：**
- `qwen-turbo` - 快速响应（默认）
- `qwen-plus` - 平衡性能
- `qwen-max` - 最强能力

### 2. OpenAI

**特点：**
- 英文能力强
- 模型选择多
- 社区支持好

**配置：**
```bash
# .env 文件
OPENAI_API_KEY=sk-your-api-key
OPENAI_BASE_URL=https://api.openai.com/v1  # 可选，用于代理
```

**可用模型：**
- `gpt-4o-mini` - 轻量级（默认）
- `gpt-4o` - 标准版
- `gpt-4-turbo` - 高性能

## 常见问题解决

### 1. API 密钥问题

**问题：** `DASHSCOPE_API_KEY未设置`

**解决方案：**
```bash
# 检查环境变量
echo $DASHSCOPE_API_KEY

# 确保在项目根目录的 .env 文件中设置了 API Key
cat ../../.env

# 或在代码中直接设置
import os
os.environ["DASHSCOPE_API_KEY"] = "your_key"
```

### 2. 模块导入错误

**问题：** `ModuleNotFoundError: No module named 'langchain'`

**解决方案：**
```bash
# 确保在正确的虚拟环境中
source ../../.venv/bin/activate

# 安装依赖
uv sync
```

### 3. 工具调用失败

**问题：** 工具执行返回错误

**解决方案：**
1. 检查输入格式是否正确
2. 查看错误信息定位问题
3. 使用演示模式测试工具功能

### 4. 响应时间过长

**问题：** LLM 响应很慢

**解决方案：**
1. 使用更快的模型（qwen-turbo）
2. 检查网络连接
3. 减少对话历史长度

## 性能优化

### 1. 使用更快的模型
```python
# 使用 turbo 版本提高响应速度
agent = NetworkEngineerAgent(
    llm_type="tongyi",
    model_name="qwen-turbo"
)
```

### 2. 控制对话历史
```python
# 定期清除对话记忆
if len(agent.memory) > 10:
    agent.clear_memory()
```

### 3. 直接调用工具
```python
# 如果明确知道要使用哪个工具，可以直接调用
from tools import LogAnalysisTool
tool = LogAnalysisTool()
result = tool.run('errors|日志内容')
```

## 扩展使用

### 1. 集成到 Web 应用

```python
from flask import Flask, request, jsonify
from agents import NetworkEngineerAgent

app = Flask(__name__)
agent = NetworkEngineerAgent(verbose=False)

@app.route("/chat", methods=["POST"])
def chat():
    question = request.json.get("question")
    response = agent.chat(question)
    return jsonify({"response": response})

if __name__ == "__main__":
    app.run(debug=True)
```

### 2. 批量处理

```python
from tools import LogAnalysisTool

tool = LogAnalysisTool()
logs = ["日志1", "日志2", "日志3"]

for log in logs:
    result = tool.run(f"errors|{log}")
    print(result)
```

---

*最后更新: 2026年2月17日*
*文档版本: v1.0*
*维护团队: build-your-own-ai项目团队*
