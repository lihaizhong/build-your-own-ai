# 迪斯尼RAG问答助手

基于LangChain和向量数据库的智能问答系统，能够基于迪斯尼相关文档进行准确的问题回答。

## 项目简介

本项目实现了一个完整的RAG（Retrieval-Augmented Generation）问答系统，通过向量数据库检索相关文档片段，结合大语言模型生成准确的答案。系统支持迪斯尼相关的知识库问答。

## 功能特性

- 📚 文档加载与处理（PDF、TXT等格式）
- 🔍 智能文本分割与向量化
- 🗄️ FAISS向量数据库存储与检索
- 💬 基于LangChain的RAG问答流程
- 🎯 支持多种大语言模型（DeepSeek、通义千问等）

## 项目结构

```
15-CASE-迪斯尼RAG助手/
├── code/           # 核心代码
├── data/           # 数据目录
│   ├── pdf/        # 原始PDF文档
│   └── processed/  # 处理后的数据
├── docs/           # 文档
├── output/         # 输出结果
├── .env.example    # 环境变量模板
├── .gitignore      # Git忽略配置
└── pyproject.toml  # 项目配置
```

## 环境要求

- Python >= 3.11
- uv 包管理器

## 快速开始

### 1. 环境配置

```bash
# 进入项目目录
cd practice/15-CASE-迪斯尼RAG助手

# 创建虚拟环境
uv venv --python 3.11

# 激活虚拟环境
source .venv/bin/activate  # Linux/Mac
# 或
.venv\Scripts\activate     # Windows

# 安装依赖
uv sync
```

### 2. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件，填写你的API密钥
```

### 3. 运行示例

```bash
# 使用uv运行
uv run python code/rag_example.py

# 或激活虚拟环境后直接运行
python code/rag_example.py
```

## 技术栈

- **LangChain**: 大模型应用开发框架
- **FAISS**: 向量相似度搜索
- **Sentence-Transformers**: 文本向量化
- **DeepSeek**: 大语言模型
- **DashScope**: 阿里云通义千问API

## 开发说明

### 添加新依赖

```bash
# 添加依赖
uv add package_name

# 添加开发依赖
uv add --group dev package_name
```

### 代码风格

项目使用 `black` 和 `ruff` 进行代码格式化：

```bash
# 格式化代码
uv run black code/

# 检查代码
uv run ruff check code/
```

## 许可证

本项目遵循项目根目录下的LICENSE文件中定义的许可证。